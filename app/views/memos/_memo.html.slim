li
  h2.date
    = memo.show_date
  .memo
    = markdown memo.text
    / p
      / = memo.text
  = link_to '編集', edit_user_memo_path(id: memo.id)
  = link_to '削除', user_memo_path(id: memo.id), method: :delete, data: {confirm: '本当に削除しますか？'}
